# See https://wiki.hyprland.org/Configuring/Keywords/ for more

$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, C, killactive, 
bind = $mainMod, Q, killactive, 
bind = $mainMod, M, exit, 
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating, 
bind = $mainMod, Space, exec, $menu
bind = $mainMod, R, exec, $menu
bind = $mainMod, F, fullscreen
bind = $mainMod SHIFT, L, exec, hyprlock
bind = $mainMod SHIFT, Q, exec, wlogout -p layer-shell
bind = $mainMod SHIFT, B, exec, ~/.config/hypr/scripts/toggle-waybar.sh
# bind = $mainMod, W, exec, google-chrome-stable
bind = $mainMod, backslash, exec, "${HOME}/.config/hypr/scripts/layout.sh"
bind = $mainMod SHIFT, backslash, exec, "${HOME}/.config/hypr/scripts/layout2.sh"
bind = $mainMod, period, exec, wofi-emoji

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Move focus with mainMod + hjkl 
bind = $mainMod, H, movefocus, l
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d
bind = $mainMod, L, movefocus, r

# screenshot keybind
bind = $mainMod, S, exec, bash -c 'grim -o DP-1 "${HOME}/Pictures/screenshots/SS$(date +%Y-%m-%d_%H-%M-%S).png" && wl-copy < "${HOME}/Pictures/screenshots/SS$(date +%Y-%m-%d_%H-%M-%S).png"'

# screenshot + crop keybind
bind = $mainMod SHIFT, S, exec, bash -c 'grim -g "$(slurp)" "${HOME}/Pictures/screenshots/SS$(date +%Y-%m-%d_%H-%M-%S).png" && wl-copy < "${HOME}/Pictures/screenshots/SS$(date +%Y-%m-%d_%H-%M-%S).png"'

# screenshot keybind (PrtSc)
bind = , Print, exec, HYPRSHOT_DIR=$HOME/Pictures/Screenshots hyprshot -m region

# toggle no border, no gaps, no border radius
bind = $mainMod SHIFT CTRL, B, exec, ~/.config/hypr/scripts/toggle-compact.sh

# Example special workspace (scratchpad) COMMENTED OUT FOR SCREENSHOT KEYBIND
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Emoji
bind = $mainMod, period, exec, rofimoji --selector wofi

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bindel = , F3, exec, wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 5%+
bindel = , F2, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl  = , F1,        exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

# Configure brightness
bind = , F6, exec, brightnessctl set 10%+
bind = , F5, exec, brightnessctl set 10%-
